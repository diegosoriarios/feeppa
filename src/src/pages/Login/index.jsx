import React from "react";
import { Link } from "react-router-dom";

const LoginPage = () => {
  return (
    <a href="/home" className="btn btn-primary">Login with google</a>
  );
};

export default LoginPage;
